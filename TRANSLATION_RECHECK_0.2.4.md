# 0.2.4 实页复验与第二轮校对

日期：2026-09-20。结论：重点数值与语义修复已生效，但不能将 0.2.4 标记为全部验收通过。本轮只检查与记录，没有修改扩展代码。

用户确认安装 0.2.4；通过新打开的侧边浏览器页面观察到对应修复。没有读取扩展安装目录，因此未进行安装副本的字节一致性核验。

## 后续 0.2.5 修复记录

以下为本轮源码修复状态，后面的 0.2.4 实页观察原样保留，不能混同为 0.2.5 实页验收结果。

| 编号 | 0.2.5 处理 |
| --- | --- |
| R01–R02 | 完整匹配支持没有子元素的相邻文本节点，保留节点身份、动态更新与关闭恢复；上标等结构保护不变。 |
| R03 | 模板增加真实的 Charm Effect Duration，使用现有官方词条；两项奖励同时回归。 |
| R04 | 分页支持可选的前导连字符及空白，原分页数值不变。 |
| R05 | 市集控件 title 加入初始及动态翻译；扩展生成的原文提示单独记录，不覆盖结果原文提示。 |
| R06 | 补 Armour／Evasion 数值字段前缀。 |
| R07 | 表单辅助属性与表单内容的保护分离；允许 aria-label、placeholder 等元数据翻译，不修改 textarea／input 的值；显式禁译及账号区域继续保护。补职业缩略图辅助名称。 |
| R08 | 实页结构为 `.property > label` 加 `80<span class="label">, Int</span> 121`；仅在需求属性内翻译缩写，不移动或替换数值节点。 |
| R09 | 调整六条快捷键说明的中文片段顺序与措辞，保留原图标、按键和强调节点；整段原文恢复纳入回归。 |
| R10 | 链接内 New 徽标显示“新”，排序按钮仍显示“最新”。 |
| R11 | 暂未修改：复杂跨列表词缀及缺少可靠译文的条目仍需独立配对，不能用模糊合并解决。 |
| R12 | 部分处理：市集合并法律链接、官网降价句尾、已核对的论坛标题和简介。其余术语、旧版伪占位 value、固定帮助及宣传正文继续保留待处理。 |

新增回归使用真实文本节点形状，而非仅测试完整字符串；覆盖双语、恢复、动态修改、输入值／数值／节点身份和原文提示保护。未修改用户浏览器中的安装副本，也未提交或推送代码。

0.2.5 验证：`npm test` 全套通过，包含引擎、数据覆盖、Chrome DOM/API、Edge MV3 加载及六站模块测试；关键脚本语法检查和 `git diff --check` 通过。0.2.5 安装后的实页复验尚未执行，Maxroll 限制不变。

## 后续 0.2.6 修复记录

R11 部分修复：Mobalytics 物品表中，两段纯文本 `li` 组成的闪电伤害上限词缀、近期装填不消耗弹药词缀，现在按完整句查询现有市集来源词库。只允许这两种已核实句式，限定 `main table` 中同一个列表，不泛化合并独立词缀，不移除网站节点。包含链接或其他富文本的列表项暂不合并。

更新任一半词缀时重扫所属列表，恢复另一半原文再匹配；未知续句不沿用旧译文。新增回归覆盖上限 500→750、双语、节点身份、关闭后完整恢复、独立生命词缀与未知续句保护。Protective Rune、格挡附加伤害的站点缩写、Chaotic Infusion 等其余 R11 项以及未完成 R12 项仍保留，不标记为全部修复。此记录为源码与受控测试范围，不代表已安装版本的实页验收；Maxroll 限制不变。

0.2.6 验证：`npm test` 各测试套件通过，包含新增跨列表回归；修改脚本语法检查与 `git diff --check` 通过。未自动替换侧边浏览器安装副本。

## 0.2.7 术语复核与适配

- 已修改：Invoker→祈靈者；NINJA Lineage Gems→族裔輔助寶石；官网 Kirac's Vault Pass→基拉克的秘寶指南；市集分类 Breach→裂痕（含原生 API 标签显示，不改分类 ID）。
- 技能历史名 Chaotic Infusion→混沌灌注，新名 Chaotic Surge→混沌波動分别保留来源；Mobalytics 的 Grants Skill 支持可选 Level 数值，未知技能不拼凑翻译。
- 核实后保留：Strongboxes→保險箱有编年史依据，加入回归，不再列为无可靠译名。Corrupted→已汙染有对应页面，不改；Interlude 页面标题仍为間歇，但正文为第六章，保留章节版本对应的待核实状态，不凭直觉换词。
- 仍待处理：Vaal Body Part、Forbidden Rites、Twice Corrupted；Protective Rune 虽找到同名防護符文页面，但尚未确认荆棘词缀中的对象相同；格挡附加伤害的缩写及其他未补齐固定文案继续保留。
- 来源明细见 SOURCES.md 与 supplemental-pairs.json。自动化验证不替代侧边浏览器安装后实页验收，Maxroll 访问限制不变。
- 验证结果：0.2.7 `npm test` 全套通过（引擎、数据覆盖、市集浏览器、MV3 加载、六站模块），新增已知／未知技能及等级、站点术语断言通过；脚本语法检查和 `git diff --check` 通过。未自动替换用户安装副本。

## 覆盖与方法

### 0.2.8 实页回归与 0.2.9 修复

0.2.8 实页确认：等级范围、Craft 提示及用户输入保护、NINJA 行情介绍与联盟状态生效。但三种跨列表词缀出现前半句恢复英文、后半项被重建为 `<br>` 的严重回归，不能验收通过。虽然不再出现 `{0}%`，腐化之血仍丢失后半句。

0.2.9 移除这三种跨列表词缀的清空续句逻辑：先从完整来源词条取译文，再按明确中文语义边界分成两段，各自在原列表项的文本节点中替换，双语原文也分别保留。仅处理每项一个非空文本节点的已验证结构，其他富文本保持原有内容，不合并压平。没有新增猜测性游戏译文。

新增回归模拟编辑器将空项转换为 `<br>`，断言扩展从未触发此转换；覆盖后半节点被网站替换、整个列表重建、数值更新、双语切换、关闭完整恢复与未知续句。0.2.9 安装后的侧边浏览器实页复验仍待执行，不能以受控测试替代。

验证记录：引擎、数据覆盖、市集浏览器及 MV3 加载测试通过；六站套件首次在首页导航超时，单独重跑 `node tests/sites.test.cjs` 通过，包含上述新增断言。脚本语法检查与 `git diff --check` 通过。未自动修改侧边浏览器安装副本。

### 0.2.7 实页发现与 0.2.8 源码修复

用户确认安装 0.2.7 后，侧边浏览器确认祈靈者、族裔輔助寶石、基拉克的秘寶指南已生效；POBb.in 的辅助标签和上标节点仍保留。复验暴露以下问题，不能将此前模拟测试通过视为这些条目实页通过：

1. 腐化之血后半句显示 `{0}%`：词库无参英文对应含参中文，被直接加入精确匹配。0.2.8 拒绝这种词条，并用既有 Bloodbarrier 完整来源匹配 5 秒与 50% 两个参数。
2. 跨列表闪电伤害和弹药词缀仍英文：实页为 `li > span`，0.2.6 测试只有纯文本 `li`。0.2.8 支持纯文本 span 包装，仍拒绝链接、上标、禁译区和未知组合；新增实际包装结构及数值变化、节点身份和恢复测试。
3. 技能授予实际为 `Level (1-20)`：新增括号等级范围，不只接受单个整数；保留整个范围。
4. Craft 两个提示为 `.init` 输入值，聚焦变 `.focused` 并缓存 `initerm`，失焦空值恢复。0.2.8 仅翻译两个指定 ID 的未聚焦 init 提示；捕获聚焦时先恢复原文，不修改默认值或 initerm。用户输入即便等于英文提示也保留。其他输入值继续跳过。
5. NINJA 首页的 Active、Previous League 与三段行情介绍补入固定界面译文。

上述为源码修复说明；0.2.8 安装后的实页复验仍待执行。未修改用户安装副本，Maxroll 仍未实页验收。

验证结果：`npm test` 全套通过，包含引擎参数拒绝、三类 span 包装跨行词缀、动态数值、双语／关闭恢复、禁译与上标保护、Craft 聚焦失焦提示状态及同文用户输入保护；脚本语法检查和 `git diff --check` 通过。

- POBb.in：公开构筑 `https://pobb.in/Jk5PC8sJXUxf`，属性截图、数值节点、配置、装备图片及辅助标签。
- Mobalytics：流派列表与筛选弹层、Navira 构筑详情、空白规划器及奖励菜单、传奇物品库 22 张表格。
- 市集：Forbidden Rites 空条件搜索结果、装备筛选、上架时间下拉、设置与关于页。
- NINJA：Forbidden Rites 货币行情与角色列表／筛选。
- Craft of Exile：beta 首页、胸甲→INT、制作条件区、词缀筛选及物品卡；旧版首页的搜索输入提示。
- 官网：商城分类、论坛目录、首页动态折扣与直播计数。
- Maxroll：前轮明确的工具安全策略限制仍是未验收项；本轮未绕过限制，也不以模拟测试代替实页通过。

动态内容在后续读取中再次核实；刚打开下拉时的英文不直接算作漏译。未进行交易、密语、黑名单删除、发布构筑、保存、购买或账号设置修改。报告不保存账号、卖家名称、私人历史和构筑代码。

## 已在真实页面复验通过

| 项目 | 实页证据 |
| --- | --- |
| POBb.in 属性上标 | 生命数字 `1`、上标 `5%`；能量护盾数字 `12,505`、上标 `271%` 各自保留在原 `span`／`sup` 中，截图确认百分比恢复上标。不是普通文本 `15%` 或 `12,505271%`。 |
| POBb.in 配置及辅助文本 | `26% 感電，3 層狂怒球，3 層暴擊球，自訂詞綴`；时间为 `2 個月前`；历史、源码链接与代表性装备图片 alt 已中文。 |
| 市集核心筛选 | `物理每秒傷害`、`元素每秒傷害`、`上架時間`；1／3／12 小时、天、周、月选项均显示“最近…內”。 |
| 市集搜索结果 | `顯示 100 筆結果（符合 10000+ 筆）`；代表性卡片的需求标签、`上週上架`、`7 天前上架` 正常。 |
| 市集危险操作标签 | 设置页显示 `移除全部`；没有点击。 |
| Mobalytics 列表／筛选 | 热门、最新、版本、本周／本月、显示 101 笔结果、关闭筛选、筛选操作、作者、更新于均已生效；导航图片辅助名称中文。 |
| Mobalytics 规划器 | 一般文字、选择奖励、剩余字数、复制代码等通过；充能奖励选项最终显示 `增加30%護符充能獲取，+1護符欄位`。 |
| Mobalytics 物品库 | 传奇分类、物理伤害／力量等数值标签已中文；固定 5% 熔火噴濺词缀实页已匹配。 |
| NINJA | `/idols` 为魔偶、`/omens` 为預兆；传奇装备分类、价格显示、物品搜索、筛选搜索、核心天赋、双手锤／权杖组合均通过。 |
| Craft beta | 计算条件、全部符合／任一符合／不符合、计算状态、否定标签、胸甲属性组合、聖物类别均通过。快捷键不再误译“击中”。 |
| Craft 物品卡 | 当前随机值为 `5(5-10)%`，显示 `生命值所受的5(5-10)%傷害由魔力扣除`；数值与范围保留。不是上一轮的 7，但覆盖相同格式。 |
| 官网 | 商城分类及图片 alt、论坛计数／最新文章／展开与部分板块、账号栏、`折扣 29%`、`1 天後結束`、观看人数已生效。 |

## 仍未生效或新发现的问题

以下编号是问题组，不是页面上全部独立缺陷的数量。

| 编号 | 优先级 | 现象、原因证据与后续建议 |
| --- | --- | --- |
| R01 | P2 | Mobalytics `set 1:`／`set 2:` 仍英文。实际 `<div>` 内是两个相邻文本节点：`set 1` 与 `:`，没有子元素。`sentences()` 的 `!el.children.length` 将其跳过，而动态模板要求完整冒号。需要“相邻文本节点、零子元素”的回归，不应直接压平任意富文本。 |
| R02 | P2 | Mobalytics `(Optional)` 仍英文；实际是 `(`、`Optional`、`)` 三个相邻文本节点，原因同 R01。已有完整词条不等于实际命中。 |
| R03 | P2 | 规划器奖励第二项 `30% increased Charm Effect Duration, +1 Charm Slot` 仍英文。同菜单第一项充能奖励已中文。源码允许的是 `Skill Effect Duration`，与实页 `Charm Effect Duration` 不同；应按真实词缀及可靠来源修正模板并补双选项回归。 |
| R04 | P2 | 市集黑名单摘要仍是 `黑名單 - Showing 1-50 of 0 (Max 1000)`。准确节点是内层 `<span>` 的 ` - Showing 1-50 of 0 (Max 1000)`，不是把“黑名单”放进同一个节点。前导连字符导致 `^Showing` 模板失败。空列表显示 1–50 属于原站现象，不能当翻译 bug 修数值。 |
| R05 | P2 | 市集布局按钮仍暴露英文 `Default`／`Compact`／`Compact Two-Columned`。它们是无文字按钮的 `title` 属性，不是普通文本；市集属性扫描不包含 title。应区分网站控件 title 与扩展保留英文的原文提示，不能一律覆盖。卡片 `Requires:` 原文 title 同理不自动算漏译。 |
| R06 | P2 | Mobalytics 物品库仍有 `Armour: 269`、`Evasion: 16` 等数值字段。当前数值标签仅从 UI 字典中找前缀，Mobalytics 未补这些键。不能根据游戏词库单词能查到就认为数值格式已覆盖。 |
| R07 | P3 | POBb.in 的 `textarea` 辅助名称仍为 `Path of Building buildcode`。无 placeholder，只有 aria-label；textarea 被整体跳过，已补词条不会生效。可以翻译固定辅助属性，但必须保护输入值。`Ascendancy Thumbnail` 也仍英文。 |
| R08 | P2 | Craft 物品卡虽显示“需求等級”，其后的 `80, Int 121` 仍混合英文。完整 `Int` 词条不覆盖带数字的组合文本；需要限制在需求组件处理，保留所有数值。 |
| R09 | P3 | Craft 操作说明词义正确但拼接措辞生硬，如“懸停〔于物品上〕…以套用…”、“右鍵…以開啟內容選單選項並操作對應元素”。应按六条完整说明安排中文语序，同时保留鼠标图标和按键节点。 |
| R10 | P3 | Mobalytics 的 New 站点覆盖也影响跨游戏导航中的新品徽标，显示“最新”。这是上下文范围仍偏大的例子；排序可用“最新”，新品徽标宜单独处理，不应仅用站点级短词覆盖宣称完全消除歧义。 |
| R11 | P2 | Mobalytics 物品库继续缺少跨 li 的长词缀、Protective Rune 词缀、每格挡率附加物理伤害、Chaotic Infusion 技能授予等；仍需来源核对及结构级处理。不能靠扩展数值匹配解决所有缺项。 |
| R12 | P2/P3 | 既有未完成范围仍存在：市集固定帮助说明和合并法律链接；NINJA 的 Lineage Gems／Vaal Body Part；官网商品降价长句、Kirac 品牌名及部分论坛板块简介；Craft 旧版伪占位输入值与 Strongboxes 术语；Mobalytics Interlude／Invoker 及固定宣传文案。它们不是本轮新回归，但不能标为修复通过。 |

旧版 Craft 两个伪占位框的实际 ID 为 `poecSearchBaseInput`、`poecSearchAffixInput`，提示存在 value，placeholder 为空。后续如处理，必须识别提示状态；不能普遍翻译输入值或修改用户搜索词。

## 第二轮量化与边界

- Mobalytics 物品库相同选择器 `main table li`，含连续至少 3 个英文字母的列表项由前轮 819 个实例变为本轮 275 个实例。减少 544 个实例，但不是修复了 544 条独立词缀；有重复项，网站内容也可能变化。
- 没有发现之前 POBb.in 上标压平在这份构筑上继续发生；不等于所有构筑结构都已覆盖。
- 本轮没有切换安装扩展的全局开关／双语设置，也未验收全部悬浮提示、移动布局、每篇攻略、每种物品与异常状态；前轮受控测试不能替代这些实页范围。
- 制作器页面显示执行耗时不代表扩展耗时；没有扩展开关对照，不对性能定责。

## 下一轮回归应采用的真实结构

1. `set 1` + `:` 与 `(` + `Optional` + `)`：相邻文本节点但没有子元素，验证更新与关闭恢复。
2. 无正文的 `button[title]`、带 aria-label 的 textarea：翻译辅助标签但不碰原文提示／输入值。
3. 含 ` - ` 前缀的分页摘要、`80, Int 121` 属性组合：严格模板，所有数字不变。
4. 同一奖励菜单内两种真实词缀：分别验证，不以另一种 Effect Duration 替代。
5. 数值字段新增覆盖同时保持 POB 上标保护，避免为了提高命中率重新引入结构压平。
